[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"

[[services]]
name = "academic-rag-app"
type = "web"
httpPort = 8000
healthCheckPath = "/health"